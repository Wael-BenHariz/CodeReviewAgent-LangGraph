name: AI Code Review Webhook

on:
  workflow_dispatch:

jobs:
  webhook-info:
    runs-on: ubuntu-latest

    steps:
      - name: Explain webhook setup
        run: |
          echo "PR reviews are handled by the FastAPI /github/webhook endpoint."
          echo "Configure a repository pull_request webhook in GitHub instead of this workflow."
